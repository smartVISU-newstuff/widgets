SUPERSIZE WIDGET for SmartVisu
* put the files in the Smartvisu/dropins folder
* then include `{% import "supersize.html" as supersize %}`
* insert `{{ supersize.supersize('') }}` between the `<h3></h3>` of a block 
